#include<stdio.h>

void main(void)
{
	
/*
//방법 1
//while 사용
	int month, days = 31;

	printf("몇 월 : ");
	scanf("%d",&month);
	
	while(month>12 || month<1)
	{
		puts("[입력오류] 달은 1~12까지만 가능합니다. 다시 입력해주세요.");
		printf("몇 월 : ");
		scanf("%d",&month);

	}

	switch(month)
	{
		case 1: case 3: case 5: case 7: case 8: case 10: case 12: 
			break;
		case 4: case 6: case 9: case 11: 
			days = 30;
			break;
		case 2: 
			days = 28;
			break;
		default : puts("그런달은 없습니다"); 	
			return;	//메인함수를 종료 = 프로그램을 종료하겠다
	}
		printf("%d월은 %d일까지 있습니다\n", month, days);
*/

/*
//방법 2
//do~while 사용
	int month, days = 31;

	do
	{
		printf("몇 월 : ");
		scanf("%d",&month);
		if(month>12 || month<1) puts("[입력오류] 달은 1~12까지만 가능합니다. 다시 입력해주세요.");
	}
	while(month>12 || month<1);
		
	switch(month)
	{
		case 1: case 3: case 5: case 7: case 8: case 10: case 12: 
			break;
		case 4: case 6: case 9: case 11: 
			days = 30;
			break;
		case 2: 
			days = 28;
			break;
		default : puts("그런달은 없습니다"); 	
			return;	//메인함수를 종료 = 프로그램을 종료하겠다
	}
		printf("%d월은 %d일까지 있습니다\n", month, days);
*/

/*
//방법 3
//while(0아닌 수) : 무한반복 사용
	int month, days = 31;
	while(-7){
		printf("몇 월 : ");
		scanf("%d",&month);
		if(month>=1 && month<=12) break;
		puts("[입력오류] 달은 1~12까지만 가능합니다. 다시 입력해주세요.");
	}
	

	switch(month)
	{
		case 1: case 3: case 5: case 7: case 8: case 10: case 12: 
			break;
		case 4: case 6: case 9: case 11: 
			days = 30;
			break;
		case 2: 
			days = 28;
			break;
		default : puts("그런달은 없습니다"); 	
			return;	//메인함수를 종료 = 프로그램을 종료하겠다
	}
		printf("%d월은 %d일까지 있습니다\n", month, days);
*/

/*
	int a, b;
	char c;
	while(100)
	{
		printf("두 수 : ");
		scanf("%d %d", &a, &b);
		printf("합은 %d", a+b);
		printf("계속하시겠습니까? 계속하려면 y, 그만두려면 n을 입력해주세요 : ");
		fflush(stdin);
		scanf("%c", &c);
		if(c=='n') break;
	}
	puts("합 계산 프로그램을 종료합니당~★");
*/





}
